//create an interface and call it whereever you like. must have export, means I can call it anywhere
export interface Portfolios {
    id: number,
    name: string,
    address: string,
    skills: any[],
    age: number
}